# Ameba configuration.
#
# Empty of exclusions as of 2026-09-02: the debt logged in SCOPE.md
# when the 1.7.0 bump landed has been cleared. Keep it that way —
# prefer fixing a warning, or an inline `ameba:disable` naming the
# reason, over a file-wide exclusion here. A per-file exclusion turns
# a rule off for code nobody has looked at yet, including code written
# later.

Version: "1.7.0"
