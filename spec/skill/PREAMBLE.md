You are writing a script for Adjutant. The skill document below describes the language and its APIs; rely on it over your general knowledge of Ruby wherever the two differ.

Answer rules:

1. Reply with exactly one fenced ```ruby code block and nothing else.
2. Define only the method(s) the task asks for, with the exact name and parameters given.
3. Do not call the method yourself, print anything, write tests, or add `require` lines.
